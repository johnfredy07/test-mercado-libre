import React from 'react';
import PropTypes from 'prop-types';
import { Detail } from '../components/Search';

const Item = () => (
  <div>
    <Detail item={{}} />
  </div>
);

Item.propTypes = {};

export default Item;
